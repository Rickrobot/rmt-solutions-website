NAV FIX — add Overhead & Gantry Crane Lift Plans to site navigation
Built: 21 May 2026

These two EDITED files REPLACE the existing versions in your repo.
Each adds one line: a link to /services/overhead-gantry-crane-lift-plans,
placed right after "Mobile Crane Lift Plans".

components/Header.js   -> adds it to the header "Services" dropdown menu
components/Footer.js   -> adds it to the footer Services column

No other changes. After deploy the new service page is reachable from
the Services menu on every page (and from the footer).
